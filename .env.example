# =============================================================================
# ADHD Calendar — przykładowa konfiguracja środowiska
# Skopiuj ten plik jako .env i wypełnij wartości przed uruchomieniem.
# NIGDY nie commituj pliku .env do repozytorium!
# =============================================================================

# -----------------------------------------------------------------------------
# PostgreSQL
# -----------------------------------------------------------------------------
POSTGRES_USER=adhd
POSTGRES_DB=adhd_calendar

# ZMIEŃ to hasło na silne, unikalne hasło przed uruchomieniem!
# Przykład generowania: python3 -c "import secrets; print(secrets.token_hex(32))"
POSTGRES_PASSWORD=adhd_secret_CHANGE_ME

# -----------------------------------------------------------------------------
# Backend — JWT
# -----------------------------------------------------------------------------
ALGORITHM=HS256

# Czas życia tokena w minutach (10080 = 7 dni)
ACCESS_TOKEN_EXPIRE_MINUTES=10080

# ZMIEŃ ten klucz na losowy ciąg min. 64 znaków przed uruchomieniem!
# Przykład generowania: python3 -c "import secrets; print(secrets.token_hex(64))"
SECRET_KEY=supersecretkey_CHANGE_ME_use_at_least_64_random_characters

# -----------------------------------------------------------------------------
# Konto demo (tworzone automatycznie przy starcie przez seed)
# Służy do szybkiego testowania aplikacji bez rejestracji.
# Usuń lub zmień przed wdrożeniem produkcyjnym.
# -----------------------------------------------------------------------------
DEMO_USER_EMAIL=demo@adhd.local
DEMO_USER_PASSWORD=demo1234

# -----------------------------------------------------------------------------
# Konto administratora (tworzone automatycznie przy starcie)
# Administrator może generować kody zaproszeń do rejestracji.
# ZMIEŃ hasło przed uruchomieniem!
# -----------------------------------------------------------------------------
ADMIN_EMAIL=admin@adhd.local
ADMIN_PASSWORD=changeme_admin_CHANGE_ME

# -----------------------------------------------------------------------------
# CORS — dozwolone originy dla frontendu
# Lokalnie: http://localhost:5173
# Na serwerze: podaj domenę/IP przez które dostępny jest frontend, np.:
#   ALLOWED_ORIGINS=https://twoja-domena.pl,https://www.twoja-domena.pl
# Wiele originów oddziel przecinkiem (bez spacji).
# -----------------------------------------------------------------------------
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# -----------------------------------------------------------------------------
# Frontend — URL backendu (używany przez Vite podczas budowania)
# Lokalnie: http://localhost:8000
# Na serwerze: podaj publiczny adres backendu
# -----------------------------------------------------------------------------
VITE_API_URL=http://localhost:8000
