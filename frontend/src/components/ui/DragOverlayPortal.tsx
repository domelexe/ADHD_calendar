import { DragOverlay } from '@dnd-kit/core'

/**
 * Global drag overlay — rendered outside of any DndContext.
 * Each DndContext handles its own DragOverlay, this file is unused but kept for future extension.
 */
export function GlobalDragOverlayPortal() {
  return null
}
